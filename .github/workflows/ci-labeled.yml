name: CI Labeled Test
on:
  pull_request:
    types: [labeled]
    branches:
      - 'features/**'

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - name: Echo the greeting's time 1
        run: echo 'The time was'
